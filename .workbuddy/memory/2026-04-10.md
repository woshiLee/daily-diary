# 2026-04-10 工作日志

## 项目结构梳理

- 对 daily-diary 项目进行了完整结构整理
- 确认项目为纯前端单文件 SPA PWA，主文件 index.html（132KB，3859行）
- 发现冗余文件：中文命名心情图（开心.png等，与 mood_xxx.png 重复）、AI测试图、示例照片
- HuiwenMincho.otf 字体文件 23.3MB，是最大文件，未来可考虑子集化
- 应用包含 6 个页面视图：首页/年度/新建/详情/设置/同步 + 登录页

## 修复测试数据加载

- 问题：本地开发时 Firebase 连接失败导致测试数据不显示
- 解决：在 `loadEntriesFromFirebase` 的 catch 块中添加本地模式，Firebase 失败时直接使用 TEST_DATA
- 修改位置：index.html 第 1506-1535 行

## 登录页UI优化

- 登录按钮间距调整：将 `.form-options` 的 `margin-bottom` 从 18px 改为 14px，与输入框间距一致
- 错误提示自适应：`.form-error` 默认不占空间（margin/height/padding 为 0），有内容时才撑开并添加间距

## 手机号验证码问题修复

- 问题：点击"获取验证码"显示"操作失败"
- 根本原因：`RecaptchaVerifier` 初始化时 `window.auth` 为 null，因为 Firebase 还未完全准备好
- 解决：
  1. 将 `initRecaptcha` 和 `initRegRecaptcha` 改为 async 函数
  2. 将 `switchAuthMethod` 和 `switchRegisterMethod` 改为 async 函数
  3. 在初始化前调用 `waitForFirebase()` 确保 Firebase 已就绪
  4. 修正 `RecaptchaVerifier` 构造函数参数顺序（auth 作为第一个参数）
- 添加更多 Firebase 错误码映射，方便问题诊断

## 账号信息交互优化

- 修改 `handleProfileClick`：点击头像区域后直接打开修改资料界面，跳过账号信息弹窗
- 用户仍可通过设置页访问退出登录功能

## UI 微调

- 设置页底部"日常书"文字颜色从 `var(--text-secondary)` 改为 `#bbb`，使其更淡更柔和
